include:
  - path: app/connector/TOTF/compose.yaml
services:
  engine:
    build:
      context: engine
      dockerfile: Dockerfile.engine
      pull: true
    environment:
      AUTHN_CONFIG_PATH: /md/auth_config.json
      ENABLE_CORS: "true"
      ENABLE_SQL_INTERFACE: "true"
      INTROSPECTION_METADATA_FILE: /md/metadata.json
      METADATA_PATH: /md/open_dd.json
      OTLP_ENDPOINT: http://local.hasura.dev:4317
    extra_hosts:
      - local.hasura.dev:host-gateway
    labels:
      io.hasura.ddn.service-name: engine
    ports:
      - 3280:3000
  otel-collector:
    command:
      - --config=/etc/otel-collector-config.yaml
    environment:
      HASURA_DDN_PAT: ${HASURA_DDN_PAT}
    image: otel/opentelemetry-collector:0.104.0
    ports:
      - 4317:4317
      - 4318:4318
    volumes:
      - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml
#   ipv6-forwarder:
#     image: alpine/socat
#     container_name: ipv6-forwarder
#     restart: unless-stopped
#     command: >
#       -d -d TCP-LISTEN:5432,fork,reuseaddr TCP6:db.lejgyavxajiwmudrnzwp.supabase.co:5432
#     ports:
#       - "65432:5432"
#     networks:
#       - totf_default

# networks:
#   totf_default:
#     external: true
